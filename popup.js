const popUp = document.querySelector('.pop-up');
const popUpButton = document.querySelector('.pop-up__button');

popUpButton.addEventListener('click', function(){

    popUp.classList.add('pop-up-closed');

});